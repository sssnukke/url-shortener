# сервер
SERVER_PORT=8080

# postgres — те же значения что в docker-compose.yml
POSTGRES_DSN=postgres://shortener:secret@localhost:5432/shortener?sslmode=disable

# redis
REDIS_ADDR=localhost:6379

# длина короткого кода (например: aB3xQz)
SHORT_CODE_LENGTH=6
