URL Shortener — сервис сокращения ссылок написанный на Go.

Принимает длинный URL, возвращает короткий код. При переходе по коду
редиректит на оригинальный адрес и считает количество переходов.

Стек: Go, PostgreSQL, Redis, Docker, Prometheus, Grafana.